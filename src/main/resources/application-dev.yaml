spring:
  application:
    name: event_management_server
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/event_management}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:021203}
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
server:
  servlet:
    context-path: ${CONTEXT_PATH:/api}
security:
  secret-key: 78040db429b610814e9f299ef8590b28be75aeacd9ff1f3e53bc8b72f4c0c88c

